import type { ReactNode } from 'react'

/** Root application component. */
function App(): ReactNode {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <h1 className="text-2xl font-bold">git-pinger</h1>
    </div>
  )
}

export default App
